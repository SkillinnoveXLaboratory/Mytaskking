name: mytaskking_mobile
description: MyTaskKing — Android + Desktop workspace.
version: 1.0.0+2
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  mytaskking_design:
    path: ../shared_design_system
  mytaskking_core:
    path: ../shared_core

  go_router: ^14.6.1
  flutter_riverpod: ^2.6.1
  agora_rtc_engine: ^6.5.0
  flutter_webrtc: ^1.5.2
  mediasfu_mediasoup_client: ^0.1.4
  socket_io_client: ^3.0.0
  permission_handler: ^11.3.1
  gal: ^2.3.0
  open_filex: ^4.5.0
  path: ^1.9.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^17.2.4
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  camera: ^0.11.0+2
  file_picker: ^8.1.4
  dio: ^5.7.0
  record: ^5.2.0
  audioplayers: ^6.1.0
  path_provider: ^2.1.5
  url_launcher: ^6.3.1
  emoji_picker_flutter: ^4.0.0
  # Plays the device's default ringtone for the incoming-call overlay so
  # the call rings the way users expect (vs a generic synth tone).
  flutter_ringtone_player: ^4.0.0+3
  # On-device speech recognition for the composer's "dictate" mic button.
  speech_to_text: ^7.0.0
  # Text-to-speech for the incoming-call availability announcement (#4).
  flutter_tts: ^4.2.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  safe_device: ^1.2.0
  device_info_plus: ^11.3.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  google_mlkit_face_detection: ^0.12.0
  proximity_sensor: ^1.4.0
  video_player: ^2.9.2

dependency_overrides:
  # record 5.x's federated linux implementation has a platform-interface
  # mismatch that fails compile even on Android. Pin record_linux to the
  # version whose signatures match record_platform_interface 1.0.x.
  record_linux: 0.7.0
  record_platform_interface: 1.0.1
  record_android: ^1.2.0
  record_darwin: ^1.0.4

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3

flutter:
  uses-material-design: true
  assets:
    - assets/icon.png
    - assets/video/call_record_guide.mp4
    - assets/sounds/Ringing.mp3.mpeg
    - assets/sounds/Buzzer.mp3.mpeg

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icon.png
  min_sdk_android: 21
