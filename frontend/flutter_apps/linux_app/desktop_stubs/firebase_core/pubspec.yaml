name: firebase_core
description: Desktop-only no-op Firebase Core stub for the Windows app.
version: 3.15.2
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
