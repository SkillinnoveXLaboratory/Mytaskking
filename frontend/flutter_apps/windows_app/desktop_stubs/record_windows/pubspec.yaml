name: record_windows
description: Desktop stub for the Windows recording plugin.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: RecordWindowsPluginCApi
